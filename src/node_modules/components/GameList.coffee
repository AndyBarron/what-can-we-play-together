React = require 'react'
sharedUtils = require 'shared/utils'

module.exports = ({games}) =>
  games = do games.slice
  games = games.sort (a, b) => sharedUtils.strcmpCaseInsensitive a.name, b.name
  <div className='component-game-list'>
    <div>{'You and the selected user(s) have the following games in common:'}</div>
    <div className='list-wrapper'>
      <div className='list'>{
        if games.length
          games.map (game) =>
            <div className='game' key={game.id} title={game.name}>
              <div>
                <div className='logo' style={{backgroundImage: "url(#{game.logoUrl})"}}></div>
              </div>
              <strong>{game.name}</strong>
            </div>
        else
          <div><br />{"None :-("}</div>
      }</div>
    </div>
  </div>
