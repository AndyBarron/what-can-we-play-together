require './styles/App'
{ BrowserRouter, Route, Switch } = require 'react-router-dom'
Login = require './pages/Login'
NotFound = require './pages/NotFound'
Profile = require './pages/Profile'
React = require 'react'
sharedConfig = require 'shared/config'

module.exports = =>
  <BrowserRouter>
    <main>
      <section className='plain'><h1>What can we play together?</h1></section>
      <Switch>
        <Route exact path='/' component={Login} />
        <Route exact path={sharedConfig.PROFILE_URL} component={Profile} />
        <Route component={NotFound} />
      </Switch>
    </main>
  </BrowserRouter>
