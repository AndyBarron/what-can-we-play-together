const LinkOut = require('components/LinkOut');
const React = require('react');
const sharedConfig = require('shared/config');

const LOGIN_URL = sharedConfig.OPENID_BASE_URL + sharedConfig.OPENID_AUTH_URL;

module.exports = (props) => (
  <div>
    <section>
      <LinkOut url={LOGIN_URL} sameTab>Log in with Steam</LinkOut>
    </section>
    <section>
      Log in with Steam to see what games you and your friends have in common!
    </section>
  </div>
);
