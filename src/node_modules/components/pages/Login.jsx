const LinkOut = require('components/LinkOut');
const React = require('react');
const sharedConfig = require('shared/config');
const styles = require('./styles/Login');

const LOGIN_URL = sharedConfig.OPENID_BASE_URL + sharedConfig.OPENID_AUTH_URL;

module.exports = (props) => (
  <div className={styles.page}>
    <section>
      <LinkOut url={LOGIN_URL} sameTab>Log in with Steam</LinkOut> to
      see what games you and your friends have in common!
    </section>
  </div>
);
